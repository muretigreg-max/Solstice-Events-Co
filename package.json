{
  "name": "inventory-sync",
  "version": "2.0.0",
  "description": "Webhook-based inventory sync, with HMAC-SHA256 signature verification. Pivoted from a polling-based prototype after the supplier deprecated its pull-based catalog endpoint in favor of push webhooks.",
  "main": "src/server.js",
  "scripts": {
    "start:receiver": "node src/server.js",
    "start:supplier": "node mockSupplier.js",
    "demo:signature": "node signatureVerification.js",
    "test": "node --test test/**/*.test.js"
  },
  "keywords": [],
  "author": "Mureti",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1"
  },
  "engines": {
    "node": ">=18"
  }
}

